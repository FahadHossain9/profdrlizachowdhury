# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# OS
.DS_Store
Thumbs.db

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Raw media folder — too large for git; processed assets go to public/
liza mam picture And uttar fertility center image and videos/

# Lab video clips are regenerated by ffmpeg pipeline — kept out of git
public/videos/lab/

# TypeScript incremental build cache
*.tsbuildinfo

# Pre-existing externally-optimised WebPs (not wired into the app)
optimized-webp/

# Env
.env
.env.local
.env.*.local
